import { Badge, Card } from '@/components/ui/Primitives';
import { Icon } from '@/components/ui/Icon';

export default function CompanyBriefPanel({ kit }) {
  const brief = kit?.company_brief || {};
  return <div className="space-y-5"><div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><Card className="p-6"><p className="eyebrow">Company brief</p><h2 className="mt-3 font-display text-3xl tracking-tight text-ink">{brief.name || 'Company workspace'}</h2><p className="mt-4 whitespace-pre-line text-sm leading-7 text-muted">{brief.summary || 'No company summary was recorded.'}</p></Card><Card className="p-6"><p className="eyebrow">Source note</p><h2 className="mt-3 text-lg font-semibold text-ink">Public research only</h2><p className="mt-3 text-sm leading-6 text-muted">This brief is grounded in the company pages supplied by the backend. Treat it as a starting point, not a substitute for your own questions.</p><div className="mt-5 flex items-center gap-2 text-sm font-medium text-brand-700"><Icon name="external" size={16} />{kit?.source?.companyUrl || 'No source URL'}</div></Card></div><Card className="overflow-hidden"><div className="border-b border-line px-5 py-4"><h2 className="font-semibold text-ink">Research sources</h2></div>{(brief.sources || []).length === 0 ? <p className="px-5 py-8 text-sm text-muted">No sources were recorded.</p> : <ul className="divide-y divide-line">{(brief.sources || []).map((source, index) => <li className="flex items-center justify-between gap-4 px-5 py-4" key={`${source.url}-${index}`}><div className="min-w-0"><p className="truncate text-sm font-medium text-ink">{source.title || 'Untitled source'}</p><p className="mt-1 truncate text-xs text-muted">{source.url}</p></div><Badge tone="gray">Source {index + 1}</Badge></li>)}</ul>}</Card></div>;
}
