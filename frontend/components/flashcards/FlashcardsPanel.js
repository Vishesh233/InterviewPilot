import { Badge, Button, Card } from '@/components/ui/Primitives';
import { Icon } from '@/components/ui/Icon';

export default function FlashcardsPanel({ kit, onPractice }) {
  const cards = kit?.flashcards || [];
  return <div className="space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="eyebrow">Flashcards</p><h2 className="mt-2 text-xl font-semibold tracking-tight text-ink">A small deck for active recall.</h2><p className="mt-1 text-sm leading-6 text-muted">Practice Mode uses the backend ordering and saves your confidence after every update.</p></div><Button onClick={onPractice} variant="primary"><Icon name="book" size={16} />Open Practice Mode</Button></div>{cards.length === 0 ? <Card className="p-8 text-center text-sm text-muted">This kit has no flashcards yet.</Card> : <div className="grid gap-4 md:grid-cols-2">{cards.map((card, index) => <Card className="p-5" key={card.id || index}><div className="flex items-center justify-between gap-3"><Badge tone="gray">Card {index + 1}</Badge><span className="text-xs text-muted">{card.practice?.confidence ? `${card.practice.confidence}/5 confidence` : 'Not rated'}</span></div><h3 className="mt-5 text-base font-semibold leading-6 text-ink">{card.front || card.term || 'Untitled card'}</h3><p className="mt-3 line-clamp-3 whitespace-pre-line text-sm leading-6 text-muted">{card.back || card.definition || 'No answer outline was recorded.'}</p><div className="mt-5 flex items-center justify-between border-t border-line pt-4 text-xs text-muted"><span>{Array.isArray(card.questionIds) ? `${card.questionIds.length} linked question${card.questionIds.length === 1 ? '' : 's'}` : 'No linked questions'}</span><span>{card.practice?.covered ? 'Covered' : 'Keep practicing'}</span></div></Card>)}</div>}</div>;
}
